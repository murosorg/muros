-r requirements.txt
pytest==8.3.3
pytest-cov==5.0.0
# Shuffles the test order on every run (and prints the seed) so any
# accidental coupling between tests surfaces as a failure instead of a
# green run that only works in alphabetical order. The suite is isolated
# per test (see tests/conftest.py::_isolate_db), so the shuffle is safe.
pytest-randomly==4.1.0
httpx==0.27.2
ruff==0.15.14

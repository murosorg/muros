fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
sqlalchemy==2.0.36
pyjwt==2.9.0
bcrypt==4.2.0
cryptography==43.0.3
qrcode==8.0
# TOTP two-factor authentication (RFC 6238) for the web UI login.
pyotp==2.9.0
python-pam==2.0.2
# python-pam 2.0.2 imports 'six' at import time but does NOT declare it
# as a dependency (packaging bug), so it must be pinned explicitly here.
# Without it `import pam` fails with ModuleNotFoundError: No module named
# 'six' and the whole PAM login is broken on a deployed appliance.
six==1.16.0

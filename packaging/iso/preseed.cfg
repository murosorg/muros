# MurOS unattended installation preseed for Debian 13 (trixie).
#
# This file is injected into the installer initrd by build-iso.sh, so it
# is read before debian-installer asks any question. The result is a
# fully unattended install of a minimal Debian system followed by the
# MurOS package install from apt.muros.org.
#
# Placeholders (@NAME@) are substituted by build-iso.sh at build time.

### Localization
d-i debian-installer/locale string en_US.UTF-8
d-i keyboard-configuration/xkb-keymap select us

### Network
# Bring up the first wired interface over DHCP. The installer needs
# network access to fetch the base system and the MurOS package.
d-i netcfg/choose_interface select auto
d-i netcfg/get_hostname string muros
d-i netcfg/get_domain string local
d-i netcfg/hostname string muros
# Do not stall if DHCP is slow or absent.
d-i netcfg/dhcp_timeout string 60
d-i netcfg/dhcpv6_timeout string 60

### Mirror
d-i mirror/country string manual
d-i mirror/http/hostname string deb.debian.org
d-i mirror/http/directory string /debian
d-i mirror/http/proxy string
d-i mirror/suite string trixie

### Clock and time zone
d-i clock-setup/utc boolean true
d-i time/zone string Etc/UTC
d-i clock-setup/ntp boolean true

### Accounts
# MurOS authenticates the web UI and SSH against the system root account
# (PAM). We therefore enable root login and create no normal user.
d-i passwd/root-login boolean true
d-i passwd/make-user boolean false
d-i passwd/root-password-crypted password @ROOT_PW_HASH@

### Partitioning
# Pick the first physical disk dynamically so the same ISO works on any
# hardware/VM regardless of the device name (sda, vda, nvme0n1, ...).
d-i partman/early_command string \
  DISK=$(list-devices disk | head -n1); \
  debconf-set partman-auto/disk "$DISK"; \
  debconf-set grub-installer/bootdev "$DISK"
d-i partman-auto/method string lvm
d-i partman-lvm/device_remove_lvm boolean true
d-i partman-md/device_remove_md boolean true
d-i partman-lvm/confirm boolean true
d-i partman-lvm/confirm_nooverwrite boolean true
d-i partman-auto-lvm/guided_size string max
d-i partman-auto/choose_recipe select atomic
d-i partman/confirm_write_new_label boolean true
d-i partman/choose_partition select finish
d-i partman/confirm boolean true
d-i partman/confirm_nooverwrite boolean true

### Base system and packages
d-i apt-setup/non-free-firmware boolean true
d-i apt-setup/contrib boolean false
d-i apt-setup/non-free boolean false
# Do NOT add the installation CD as an apt source. The CD is not mounted
# in the installed system (nor in the in-target chroot), so a lingering
# "deb cdrom:" line makes apt-get update fail with exit code 100 when the
# late_command installs MurOS. The network mirror above is the only source.
d-i apt-setup/cdrom/set-first boolean false
d-i apt-setup/cdrom/set-next boolean false
d-i apt-setup/cdrom/set-failed boolean false
d-i apt-setup/use_mirror boolean true
tasksel tasksel/first multiselect standard
# kbd + console-setup ship loadkeys/setupcon so an operator at the
# physical console can switch the keyboard layout (e.g. "loadkeys fr")
# even before configuring it persistently from the MurOS UI.
d-i pkgsel/include string curl ca-certificates gnupg kbd console-setup
d-i pkgsel/upgrade select full-upgrade
popularity-contest popularity-contest/participate boolean false

### Bootloader
d-i grub-installer/only_debian boolean true

### Finish
d-i finish-install/reboot_in_progress note

### Install MurOS from the signed apt repository.
# in-target runs inside the freshly installed system, which already has
# working network/DNS, so the official installer can register
# apt.muros.org and pull the muros package and all its dependencies.
d-i preseed/late_command string \
  in-target sh -c 'curl -fsSL https://apt.muros.org/install.sh | bash'
